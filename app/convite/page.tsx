"use client"; import Link from "next/link"; import { FormEvent, Suspense, useState } from "react"; import { useSearchParams } from "next/navigation"; import { AuthFrame } from "@/components/auth/AuthFrame"; import { useAuth } from "@/components/auth/AuthProvider";
function InviteForm(){const {request}=useAuth();const params=useSearchParams();const [message,setMessage]=useState("");const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget);try{await request("/invitations/accept",{method:"POST",body:JSON.stringify(Object.fromEntries(f))});setMessage("Convite aceito. Sua conta está pronta.")}catch(x){setError(x instanceof Error?x.message:"Convite inválido")}}return <AuthFrame title="Aceitar convite" subtitle="Complete seus dados para entrar no workspace.">{message?<div className="authSuccess">{message}<Link className="primary" href="/login">Fazer login</Link></div>:<form className="authForm" onSubmit={submit}><label>Token do convite<input required name="token" defaultValue={params.get("token")||""}/></label><label>Nome<input required name="name"/></label><label>CREA (se aplicável)<input name="crea"/></label><label>Senha<input required minLength={10} type="password" name="password"/></label>{error&&<p className="formError">{error}</p>}<button className="primary">Aceitar convite</button></form>}</AuthFrame>}
export default function Invite(){return <Suspense fallback={<main className="authScreen"><div className="authLoading">Carregando convite…</div></main>}><InviteForm/></Suspense>}
