"use client"; import Link from "next/link"; import { FormEvent, useState } from "react"; import { useRouter } from "next/navigation"; import { AuthFrame } from "@/components/auth/AuthFrame"; import { useAuth } from "@/components/auth/AuthProvider";
export default function Signup(){const {request,save}=useAuth();const router=useRouter();const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const f=new FormData(e.currentTarget);try{const d=await request<{accessToken:string;refreshToken:string;user:never}>("/auth/onboarding",{method:"POST",body:JSON.stringify(Object.fromEntries(f))});save(d);router.push("/app")}catch(x){setError(x instanceof Error?x.message:"Falha no cadastro")}finally{setBusy(false)}}return <AuthFrame title="Crie seu workspace" subtitle="Comece com uma organização isolada para sua equipe de engenharia." footer={<>Já possui conta? <Link href="/login">Fazer login</Link></>}><form className="authForm" onSubmit={submit}><div className="authGrid"><label>Organização<input required name="organizationName"/></label><label>Identificador<input required name="slug" pattern="[a-z0-9\-]{3,48}" placeholder="engenharia-silva"/></label><label>Seu nome<input required name="name"/></label><label>CREA<input required name="crea"/></label></div><label>E-mail<input required type="email" name="email"/></label><label>Senha<input required minLength={10} type="password" name="password"/></label>{error&&<p className="formError">{error}</p>}<button className="primary" disabled={busy}>{busy?"Criando…":"Criar workspace"}</button><small className="formHint">Plano gratuito nesta fase. Nenhuma cobrança será realizada.</small></form></AuthFrame>}
