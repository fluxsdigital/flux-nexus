"use client";
import { FormEvent, useState } from "react"; import { Icon } from "../../../components/Icon"; import { useSystem } from "../../../components/system/SystemProvider";
export default function Companies(){const {companies,equipment,addCompany}=useSystem();const [open,setOpen]=useState(false);const [error,setError]=useState("");const [busy,setBusy]=useState(false);const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setBusy(true);setError("");const d=new FormData(e.currentTarget);try{await addCompany({name:String(d.get("name")),cnpj:String(d.get("cnpj")),city:String(d.get("city")),contact:String(d.get("contact"))});setOpen(false)}catch(x){setError(x instanceof Error?x.message:"Não foi possível salvar a empresa")}finally{setBusy(false)}};return <><div className="pageHead"><div><small>CADASTROS</small><h1>Empresas</h1><p>Clientes e locais sob sua responsabilidade.</p></div><button className="primary" onClick={()=>{setError("");setOpen(true)}}><Icon name="plus"/>Nova empresa</button></div><section className="panel"><div className="table"><div className="tr th"><span>Empresa</span><span>CNPJ</span><span>Cidade</span><span>Equipamentos</span></div>{companies.map(c=><div className="tr" key={c.id}><span><b>{c.name}</b><small>{c.contact}</small></span><span>{c.cnpj}</span><span>{c.city}</span><span>{equipment.filter(e=>e.companyId===c.id).length}</span></div>)}</div></section>{open&&<div className="modal" onMouseDown={()=>!busy&&setOpen(false)}><form className="dialog" onSubmit={submit} onMouseDown={e=>e.stopPropagation()}><div className="panelHead"><div><small>NOVO CADASTRO</small><h2>Adicionar empresa</h2></div><button type="button" className="close" onClick={()=>setOpen(false)}>×</button></div><label>Razão social<input required name="name" placeholder="Nome da empresa"/></label><div className="formGrid"><label>CNPJ<input required name="cnpj" placeholder="00.000.000/0000-00"/></label><label>Cidade / UF<input required name="city" placeholder="Cidade/UF"/></label></div><label>Contato responsável<input required name="contact" placeholder="Nome do contato"/></label>{error&&<p className="formError">{error}</p>}<button className="primary" type="submit" disabled={busy}><Icon name="save"/>{busy?"Salvando…":"Salvar empresa"}</button></form></div>}</>}
