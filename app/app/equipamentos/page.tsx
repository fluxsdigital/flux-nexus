"use client";
import { FormEvent, useState } from "react"; import { Icon } from "../../../components/Icon"; import { useSystem } from "../../../components/system/SystemProvider";
export default function Equipment(){const {companies,equipment,companyFilter,addEquipment}=useSystem();const [open,setOpen]=useState(false);const visible=equipment.filter(e=>companyFilter==="all"||e.companyId===companyFilter);const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const d=new FormData(e.currentTarget);addEquipment({tag:String(d.get("tag")),name:String(d.get("name")),companyId:String(d.get("company")),category:String(d.get("category")),status:"Em dia",next:String(d.get("next"))});setOpen(false)};return <><div className="pageHead"><div><small>ATIVOS NR-13</small><h1>Equipamentos</h1><p>Cadastro essencial dos equipamentos inspecionados.</p></div><button className="primary" onClick={()=>setOpen(true)}><Icon name="plus"/>Novo equipamento</button></div><section className="panel"><div className="cardsList">{visible.map(e=><article key={e.id}><div className="assetIcon"><Icon name="equipment"/></div><div className="grow"><small>{e.tag} · {e.category}</small><h3>{e.name}</h3><p>{companies.find(c=>c.id===e.companyId)?.name}</p></div><div className="assetDate"><small>PRÓXIMA INSPEÇÃO</small><b>{e.next}</b></div><span className={`badge ${e.status==="Vencido"?"red":e.status==="Atenção"?"amber":"green"}`}>{e.status}</span></article>)}</div></section>{open&&<div className="modal" onMouseDown={()=>setOpen(false)}><form className="dialog" onSubmit={submit} onMouseDown={e=>e.stopPropagation()}><div className="panelHead"><div><small>NOVO ATIVO</small><h2>Cadastrar equipamento</h2></div><button type="button" className="close" onClick={()=>setOpen(false)}>×</button></div><label>Empresa<select required name="company">{companies.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><div className="formGrid"><label>TAG<input required name="tag" placeholder="VP-001"/></label><label>Categoria<select name="category"><option>Vaso de pressão</option><option>Caldeira</option><option>Tubulação</option></select></label></div><label>Nome do equipamento<input required name="name" placeholder="Ex.: Vaso pulmão de ar"/></label><label>Próxima inspeção<input required name="next" type="date"/></label><button className="primary" type="submit"><Icon name="save"/>Salvar equipamento</button></form></div>}</>}
