"use client";
import Link from "next/link"; import { usePathname } from "next/navigation"; import { useState } from "react"; import { Icon } from "../Icon"; import { useSystem } from "./SystemProvider";
const links=[[/app$/,"/app","home","Visão geral"],[/empresas/,"/app/empresas","building","Empresas"],[/equipamentos/,"/app/equipamentos","equipment","Equipamentos"],[/inspecao/,"/app/inspecao","clipboard","Nova inspeção"],[/laudos/,"/app/laudos","file","Laudos"]] as const;
export function SystemShell({children}:{children:React.ReactNode}){const path=usePathname();const [open,setOpen]=useState(false);const {companies,companyFilter,setCompanyFilter}=useSystem();return <div className="sys"><aside className={open?"sysSide open":"sysSide"}><Link className="sysLogo" href="/"><span>N</span><b>NR NEXUS<small>INSPEÇÕES NR-13</small></b></Link><nav>{links.map(([match,href,icon,label])=><Link key={href} href={href} onClick={()=>setOpen(false)} className={match.test(path)?"active":""}><Icon name={icon}/><span>{label}</span></Link>)}</nav><div className="sysUser"><span>JS</span><div><b>João Santos</b><small>Engenheiro inspetor</small></div></div></aside><main className="sysMain"><header className="sysTop"><button className="menuBtn" onClick={()=>setOpen(!open)} aria-label="Menu"><Icon name="menu"/></button><div><small>EMPRESA</small><select value={companyFilter} onChange={e=>setCompanyFilter(e.target.value)}><option value="all">Todas as empresas</option>{companies.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></div><div className="sysTopActions"><span className="online">● SISTEMA ONLINE</span><button aria-label="Notificações"><Icon name="bell"/></button></div></header><div className="sysContent">{children}</div></main></div>}
